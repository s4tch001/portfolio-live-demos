# Security policy

This is a private demo-only repository. Do not commit credentials, production endpoints, real user data, database files, uploads, access tokens, or service-role keys.

Report a suspected issue through a private issue in this repository. Include reproduction steps, affected paths, and impact, but redact credentials and personal data.

Every change must pass npm run check. Dependency updates remain subject to review even when they are opened automatically.

